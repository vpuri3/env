[user]
	name  = Vedant Puri
	email = vpuri3@illinois.edu
[core]
	editor = vim
[diff]
	tool = vimdiff
[alias]
	alog = "log --graph --oneline --all --decorate"
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
